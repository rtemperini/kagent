env/
*.log
.git/
.gitignore
.coverage
htmlcov/
workspace/
node_modules/
# Build output — the builder stage produces its own, and a stale local copy
# would only bloat the build context.
dist/
# Test artifacts.
test-results/
playwright-report/
